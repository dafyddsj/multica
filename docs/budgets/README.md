# Budgets

Plan and type sketch for workspace spend caps on Analytics.

Start at [overview.md](overview.md). The contract is [design.md](design.md). Do not implement until the design checkpoint is accepted. Owner and admin can waive project or initiative teeth for a time window. Agent pause still wins.

This folder is not product docs. Product copy will land in `apps/docs/content/docs/` in [phase 9](phase-9-reconcile-and-docs.md).

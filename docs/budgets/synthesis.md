# Arena record

Parent model was Cursor Grok 4.6. `kimi-k3-max` was configured for architect runners and is not available in this environment, so the arena ran three candidates.

| Candidate | Model | Shape |
| --- | --- | --- |
| 1 | Claude Fable | Ledger. `Admit` at claim. Project and initiative hold at claim. |
| 2 | GPT-5.6 Sol | Sealed ledger. `DecideTask` at enqueue and claim. Project budgets pause agents. |
| 3 | Cursor Grok 4.6 | Observe overlay. Client-priced bars. `Admit` always proceeds in v1. |

Cross-judge (Claude Opus 5) scored 18 / 17 / 13 and picked candidate 1. The parent pick matched.

Grafts and rejections are in [design.md](design.md) under Synthesis decision. Raw runner output stayed in `/tmp/arena-budgets/` on the authoring machine and is not in git.
